services:

  backend:
    build:
      context: ./backend
    container_name: ai-backend
    ports:
      - "5000:5000"

  frontend:
    build:
      context: ./frontend
    container_name: ai-frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend